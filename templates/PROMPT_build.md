# Ralph Loop — Build Mode

You are running inside a Ralph Wiggum autonomous loop (Context A).

Read `.specify/memory/constitution.md` — it contains all project principles, workflow
instructions, work sources, and completion signal requirements.

## Work Source Priority

Select the highest-priority incomplete work item using this lookup order:

1. **`work-items.json`** (if present) — machine-readable work queue generated by plan
   mode. Parse the JSON, skip items with `"status": "done"`, and pick the item with
   the lowest `priority` number among those that have no unfinished `dependencies`.
2. **`IMPLEMENTATION_PLAN.md`** (if present) — human-readable execution index.
   Scan for the first incomplete task or feature section.
3. **`specs/`** folder — read specs in numerical order and pick the first whose
   acceptance criteria are not yet satisfied.

## Implementing the Work Item

Implement the selected item completely, then verify all acceptance criteria and run any
required tests (guided by the item's `verification` list if sourced from
`work-items.json`, or by the spec's Completion Signal section otherwise).

After a successful implementation, commit and push, increment the item's `retry_count`
in `work-items.json` only when the iteration did NOT complete successfully, and set
`"status": "done"` in `work-items.json` when you successfully finish it.

Output the appropriate terminal promise tag:

- `<promise>DONE</promise>` when the work item is complete
- `<promise>BLOCKED:reason</promise>` when a real blocker requires human help
- `<promise>DECIDE:question</promise>` when a human decision is required
